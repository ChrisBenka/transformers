import torch
from torch import nn


class Decoder(nn.Module):
    def __init__(self, target_vocab_size, d_model, num_layers, max_seq_len, num_heads, num_ff_layers,
                 num_ff_hidden_units, device):
        pass

    def forwward(self):
        pass
